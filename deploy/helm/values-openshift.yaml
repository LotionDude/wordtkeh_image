replicaCount: 2
image:
  repository: registry.example.com/wordtkeh
  tag: "1.0.0"
route:
  enabled: true
  host: wordtkeh.example.com
  annotations:
    haproxy.router.openshift.io/hsts_header: max-age=31536000
  tls:
    enabled: true
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
# config inherits the complete production configuration from values.yaml.
# Change config.epochDate only before publication. No fixed UID/fsGroup is set.
